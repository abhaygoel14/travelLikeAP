export const FEATURE_FLAGS = Object.freeze({
  memoryGallery: false,
  travelSnapshotCard: false,
  adminTourPortal: true,
  adminTourPreview: true,
});

export const APP_CONFIG = Object.freeze({
  currencySymbol: "₹",
});
